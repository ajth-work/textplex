Bundled Chinese lexicon source for TextPlex.

This folder vendors the full vocabulary and character reference databases from the
Chinese Character Recognition project so TextPlex can import and query them locally
without depending on an external repository at runtime.

The API import endpoint defaults to this folder.
